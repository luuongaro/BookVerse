package com.grupo3.BookVerse.features.chapters.service.impl;

public class ChapterServiceImpl {
}
/**
StoryEntity story = storyRepository
        .findByIdExternal(requestDto.getStoryId())
        .orElseThrow(() -> new ResourceNotFoundException("Story not found"));

ChapterEntity chapter = chapterMapper.toEntity(requestDto);
chapter.setStory(story);
 */

